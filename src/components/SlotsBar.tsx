"use client"

// import {} from  ''

export default function SlotsBar() {

	// TODO {import from server}base colors array, map that array to build a bar with animation included
	// Here only rendering and waiting for broadcast from server to receive switch on animation
	
	return (
		<div>

		</div>
	)
}